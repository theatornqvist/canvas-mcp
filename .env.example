# Canvas LMS Configuration
# Copy this file to .env and fill in your actual values

# Your Canvas access token (get from canvas.chalmers.se → Account → Settings → New Access Token)
CANVAS_TOKEN=your_canvas_token_here

# Canvas API base URL (default for Chalmers)
CANVAS_BASE_URL=https://canvas.chalmers.se/api/v1
